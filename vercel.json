{
  "version": 2,
  "builds": [
    { "src": "vite.config.ts", "use": "@vercel/static-build" }
  ],
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/server/(.*)", "dest": "/server/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
